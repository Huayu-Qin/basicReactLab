const MovieCard = ({ movie }) => {
    // const movie = props.movie
    // const {movie} = props
    return (
        <div>
            <p>title: {movie.title}</p>
            ...
        </div>
    )
}

export default MovieCard